{
    "buildCommand": "cd student-portal && npm install && npm run build",
    "outputDirectory": "student-portal/dist",
    "framework": null,
    "installCommand": "npm install",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}