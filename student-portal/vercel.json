{
    "installCommand": "cd .. && npm install --prefix shared || true && cd student-portal && npm install",
    "buildCommand": "npm run build",
    "outputDirectory": "dist",
    "framework": "vite",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}